<h1>Animation Hub</h1>
<p>Your destination for awesome animations!</p>

<% if !user_signed_in? %>
  <p>New to AnimationHub? <%= button_to 'Sign Up', new_user_registration_path, :class => 'btn' %></p>
  <p>Already a member? <%= button_to 'Sign in', new_user_session_path, :class => 'btn' %></p>
<% end %>